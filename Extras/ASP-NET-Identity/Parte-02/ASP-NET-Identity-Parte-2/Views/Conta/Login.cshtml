@model ASP_NET_Identity_Parte_2.ViewModels.ContaLoginViewModel

@{
    ViewBag.Title = "Login";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Login</h2>

@using(Html.BeginForm())
{
    @Html.ValidationSummary("", new { @classe = "text-danger" })

    @Html.EditorForModel()

    <input type="submit" value="Login" />
}