@* importando uma classe *@
@using LojaRazor.Internacionalização

<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        @Html.ActionLink("Loja Razor", "Index", "Home", null, new { @class = "brand" })
        <ul class="nav">
            <li>@Html.ActionLink(Mensagens.Sobre, "About", "Home")</li>
            <li>@Html.ActionLink(Mensagens.Contato, "Contato", "Home")</li>
        </ul>
        <p class="navbar-text pull-right">
            @if (Session["usuarioLogado"] != null)
            {
                @Mensagens.BemVindo
                @Session["usuarioLogado"]
                @Html.ActionLink(Mensagens.Logout, "Logout", "Autenticacao")
            }
            else
            {
                @Mensagens.OlaVisitante
                @Html.ActionLink(Mensagens.Login, "Index", "Autenticacao")
                @Mensagens.Ou
                @Html.ActionLink(Mensagens.CadastraSe, "Form", "Usuarios")
            }
        </p>
    </div>
</div>

@* Para internacionalizar o meu sistema, devo criar um arquivo do tipo resource file (Mensagens.resx) e nele colocar
    as frases nas diversas lingugas (a estrutura é do tipo chave-valor/dicionario)
    
    Para criar um dicionário de mensagens em ingles, devo cirar um novo resource file Mensagens.en-us.resx*@
