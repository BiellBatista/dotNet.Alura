@addTagHelper *, 06-05-XX-CasaDoCodigo
@using _06_05_XX_CasaDoCodigo.Areas.Catalogo.Models.ViewModels
@model CarrosselPaginaViewModel

<div class="item @(Model.Indice == 0 ? "active" : "")">
    <div class="row">
        @foreach (var produto in Model.Produtos)
        {
            <vc:produto-card produto="@produto"/>
        }
    </div>
</div>